{
  "name": "daily-wlog",
  "private": true,
  "scripts": {
    "generate:api-types": "openapi-typescript openapi/openapi.yaml -o apps/mobile/src/api/types.gen.ts",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "generate:theme": "pnpm --filter mobile generate:theme",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "openapi-typescript": "^7.9.1"
  },
  "packageManager": "pnpm@10.32.1"
}
