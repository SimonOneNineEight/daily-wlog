// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: categories.sql

package dbgen

import (
	"context"
)

const insertCategory = `-- name: InsertCategory :one
insert into categories (user_id, parent_id, name, color, icon, position)
values (
    $1::uuid,
    $2::uuid,
    $3,
    $4,
    $5,
    (
        select coalesce(max(position), 0) + 1
        from categories
        where user_id = $1::uuid
          and parent_id is not distinct from $2::uuid
    )
)
returning id, position
`

type InsertCategoryParams struct {
	UserID   string
	ParentID *string
	Name     string
	Color    string
	Icon     string
}

type InsertCategoryRow struct {
	ID       string
	Position int32
}

// Position lands after existing siblings (same parent level). Name
// uniqueness per (user, parent) is enforced by the table's UNIQUE NULLS NOT
// DISTINCT constraint; the handler maps that violation to 409.
func (q *Queries) InsertCategory(ctx context.Context, arg InsertCategoryParams) (InsertCategoryRow, error) {
	row := q.db.QueryRow(ctx, insertCategory,
		arg.UserID,
		arg.ParentID,
		arg.Name,
		arg.Color,
		arg.Icon,
	)
	var i InsertCategoryRow
	err := row.Scan(&i.ID, &i.Position)
	return i, err
}

const subcategoryIsUsable = `-- name: SubcategoryIsUsable :one
select exists (
    select 1
    from categories
    where id = $1::uuid
      and user_id = $2::uuid
      and parent_id = $3::uuid
) as usable
`

type SubcategoryIsUsableParams struct {
	SubcategoryID string
	UserID        string
	CategoryID    string
}

// A usable Entry refinement: owned by the user and a child of exactly the
// Entry's Category.
func (q *Queries) SubcategoryIsUsable(ctx context.Context, arg SubcategoryIsUsableParams) (bool, error) {
	row := q.db.QueryRow(ctx, subcategoryIsUsable, arg.SubcategoryID, arg.UserID, arg.CategoryID)
	var usable bool
	err := row.Scan(&usable)
	return usable, err
}
