# IconButton
Icon-only control for nav bars, card affordances and sheet dismissal.

```jsx
<IconButton icon="chevron-left" label="Previous month" />
<IconButton icon="ellipsis" variant="filled" size="small" label="More" />
```

Always pass `label`. `variant="filled"` gives the grey circle used over content.
