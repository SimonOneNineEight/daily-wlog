# AddButton
The persistent "+" that starts today's entry — one per screen, bottom right.

```jsx
<AddButton onClick={newEntry} />
```

It gets its emphasis from the round dark fill and the `--shadow-fab` depth, never from an accent hue. Use `floating={false}` to place it inline in a bar.
