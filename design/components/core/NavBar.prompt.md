# NavBar
Top bar for a screen. `large` for the landing month title, plain for pushed screens.

```jsx
<NavBar large title="August" subtitle="2026" trailing={<IconButton icon="list" label="Year" />} />
```
