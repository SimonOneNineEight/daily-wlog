# ColorPresetPicker
The ten muted presets plus one custom swatch, for creating or editing a category.

```jsx
<ColorPresetPicker value={color} onChange={setColor} custom="#7C6E8A" onCustom={openPicker} />
```

Selection is a near-black ring around the swatch — never a change of the swatch's own hue.
