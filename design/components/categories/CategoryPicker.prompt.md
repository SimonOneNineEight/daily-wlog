# CategoryPicker
First field of the entry form: pick a category, or type a new name and create it inline.

```jsx
<CategoryPicker categories={categories} value={category} onChange={setCategory} onCreate={createCategory} />
```

The "Create …" row only appears when the query matches nothing exactly. Keep the whole path — category, title, save — under a minute.
