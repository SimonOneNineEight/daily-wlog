# DayCell
One day in the month grid. Normally used through `MonthGrid`.

```jsx
<DayCell day={17} today colors={["green", "clay"]} />
```

Today is a filled near-black circle with a white semibold numeral; the selected day is a 1.5px ring. Both are semibold — state is never carried by color alone.
