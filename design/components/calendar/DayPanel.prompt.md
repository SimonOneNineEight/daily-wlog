# DayPanel
The panel beneath the grid listing the selected day's entry titles.

```jsx
<DayPanel dateLabel="Wednesday, 12 August" entries={dayEntries} onOpenDay={openDay} onOpenEntry={openEntry} />
```

Empty days say "No entries" in grey — no illustration, no encouragement copy.
